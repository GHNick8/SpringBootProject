spring.application.name=SpringBootProject

# server port @8080
server.port=8080

# local database h2 config
spring.datasource.url=jdbc:h2:file:./db/blogdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=admin
spring.datasource.password=46zv6gz8$$gbe
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# local database h2 console
spring.h2.console.enabled=true
spring.h2.console.path=/db-console
spring.h2.console.settings.web-allow-others=false

# local h2 tables -> use value 'none' in production
spring.jpa.hibernate.ddl-auto=create-drop

# enable lazy loading
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true
